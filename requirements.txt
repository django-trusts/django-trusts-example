# Python >=3.12, Django 6.1.x — matches modernized django-trusts 1.0.0.dev0.
# Trusts is not a published PyPI 1.0; pin the post-#24 (TrustGroup) merge on master.
Django>=6.1,<6.2
django-trusts @ git+https://github.com/django-trusts/django-trusts.git@b5d5ae18e1fbe4901d0350a82a2aab1dcac92a20
gunicorn>=23.0.0
# Pure-Python driver: mysqlclient has no Linux wheels and needs client headers
# the stock Herokuish Python buildpack does not provide.
# 1.2.0+ reports MySQLdb.version_info >= (2, 2, 1), which Django 6.1 requires.
# 1.1.1 reports 1.4.6 and raises ImproperlyConfigured at backend import.
PyMySQL[rsa]>=1.2.0
dj-database-url>=2.3.0
whitenoise>=6.9.0
